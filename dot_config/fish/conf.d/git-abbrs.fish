# Git abbreviations - these expand in the command line
abbr -a ga git add
abbr -a gaa git add --all
abbr -a msg magic_diff
abbr -a gac 'git add --all && magic_diff && git commit'
abbr -a gc git commit
abbr -a gs git status
abbr -a gd git diff
abbr -a push git push
abbr -a pull git pull

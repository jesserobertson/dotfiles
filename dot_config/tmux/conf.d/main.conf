# -- theming ------------------------------------------------------------------
# Using dracula/tmux plugin for theming (configured in plugins.conf)
# Disabled old mooks theme system in favor of dracula plugin
set-option -g @tmux-theme-status-enable 'false'

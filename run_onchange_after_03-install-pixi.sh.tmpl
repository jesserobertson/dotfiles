{{- if eq .chezmoi.os "windows" -}}
#!/bin/sh
exit 0
{{- else -}}
#!/bin/bash
set -euo pipefail
# pixi packages: python uv ruff mypy requests
command -v pixi >/dev/null 2>&1 || { echo "pixi not installed, skipping"; exit 0; }
bash "{{ .chezmoi.sourceDir }}/scripts/install-pixi.sh"
{{- end -}}
